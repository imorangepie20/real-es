# 단계 1: 인증 기반 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** real-es에 멀티테넌트 인증(회원가입·로그인·로그아웃·보호 라우트)을 올려 "로그인되는 앱"을 만든다.

**Architecture:** 단계 0의 `Agency` 위에 `User`/`Session` 모델을 추가하고, `src/lib/auth/`에 격리된 인증 코어(해싱·세션 토큰·쿠키·현재 유저)를 둔다. 회원가입은 셀프서비스(Agency+admin User 동시 생성). 세션은 opaque 랜덤 토큰을 httpOnly 쿠키로, DB엔 sha256 해시만 저장. 인증은 Next 서버 액션, 보호는 `(dashboard)` 레이아웃의 서버 사이드 가드.

**Tech Stack:** Next.js 16 (App Router, 서버 액션) · React 19 (`useActionState`) · TypeScript · Prisma 6 · PostgreSQL(real_es) · bcryptjs · zod · vitest(단위/통합) · Playwright(e2e) · pnpm

## Global Constraints

- 스택 변경 금지: Next.js 16 + React 19 + TypeScript + Tailwind v4 + shadcn/Base UI + pnpm
- 앱 포트 **3001** (3000은 my-forever-music 점유)
- DB는 기존 `mrms-pg`의 `real_es` (localhost:5433, user `mrms`) — 단계 0 `DATABASE_URL` 재사용
- Prisma는 **6.x 유지** (7로 올리지 않음)
- pnpm 11: 네이티브 build script는 `pnpm-workspace.yaml`의 `allowBuilds`에서 명시 승인해야 동작 (단계 0에서 prisma 승인함)
- 멀티테넌트: `Agency`가 테넌트 루트. 첫 가입자 `role="admin"`
- 이번 단계 비범위: 비밀번호 재설정/이메일 인증/권한 세분화/직원 초대 (템플릿의 `forgot-password`·`reset-password`·`verify` 페이지는 미연결로 둔다)
- 설계 출처: `docs/superpowers/specs/2026-06-23-stage-1-auth-design.md`

---

## File Structure

- Modify `prisma/schema.prisma` — `User`·`Session` 모델 + `Agency.users` 관계
- (자동 생성) `prisma/migrations/**` — `init_auth`
- Create `src/lib/auth/password.ts` — `hashPassword`/`verifyPassword`
- Create `src/lib/auth/password.test.ts` — 단위 테스트
- Create `src/lib/auth/session.ts` — `createSession`/`validateSessionToken`/`invalidateSession`
- Create `src/lib/auth/session.test.ts` — 통합 테스트(real_es)
- Create `src/lib/auth/cookies.ts` — 세션 쿠키 set/clear/get
- Create `src/lib/auth/current-user.ts` — `getCurrentUser`
- Create `src/app/(auth)/actions.ts` — `signupAction`/`loginAction`/`logoutAction` ("use server")
- Modify `src/app/(auth)/login/page.tsx` — 서버 액션 연결
- Modify `src/app/(auth)/register/page.tsx` — 상호명 필드 추가 + 서버 액션 연결
- Modify `src/app/(auth)/layout.tsx` — 로그인 상태면 대시보드로 리다이렉트
- Modify `src/app/(dashboard)/layout.tsx` — 서버 사이드 인증 가드
- Create `src/components/auth/logout-button.tsx` — 로그아웃 버튼
- Modify `src/components/layout/app-header.tsx` — 헤더에 로그아웃 버튼
- Create `vitest.config.ts`, `vitest.setup.ts` — 단위/통합 테스트 러너
- Modify `package.json` — deps + `test:unit` 스크립트
- Modify `pnpm-workspace.yaml` — `esbuild` build 승인(vitest용)
- Create `e2e/global.setup.ts` — Playwright 인증 픽스처(유저 등록 → storageState 저장)
- Modify `playwright.config.ts` — setup/chromium(인증됨)/auth(미인증) 프로젝트 분리
- Modify `.gitignore` — `playwright/.auth/` 제외
- Create `e2e/auth.spec.ts` — 인증 E2E (미인증 컨텍스트)

---

## Task 1: Prisma User/Session 스키마 + 마이그레이션 + 런타임 의존성

**Files:**
- Modify: `prisma/schema.prisma`
- Modify: `package.json`, `pnpm-lock.yaml`
- (자동) `prisma/migrations/<ts>_init_auth/`

**Interfaces:**
- Produces: `real_es`에 `User`(id/agencyId/email/passwordHash/name/role/createdAt), `Session`(id/userId/tokenHash/expiresAt/createdAt) 테이블. `@prisma/client`에 `User`/`Session` 타입.

- [ ] **Step 1: 런타임 의존성 설치 (순수 JS — build script 없음)**

```bash
cd /opt/real-es
pnpm add bcryptjs zod
pnpm add -D @types/bcryptjs
```
Expected: `package.json`에 `bcryptjs`·`zod`(deps), `@types/bcryptjs`(devDeps) 추가.

- [ ] **Step 2: `prisma/schema.prisma`에 모델 추가**

기존 `Agency` 모델에 `users` 관계 한 줄을 추가하고, 파일 끝에 `User`·`Session`을 추가한다.

`Agency` 모델을 다음으로 교체:
```prisma
model Agency {
  id        String   @id @default(cuid())
  name      String
  createdAt DateTime @default(now())
  users     User[]
}
```

파일 끝에 추가:
```prisma
model User {
  id           String    @id @default(cuid())
  agencyId     String
  agency       Agency    @relation(fields: [agencyId], references: [id])
  email        String    @unique
  passwordHash String
  name         String?
  role         String    @default("member")
  createdAt    DateTime  @default(now())
  sessions     Session[]
}

model Session {
  id        String   @id @default(cuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  tokenHash String   @unique
  expiresAt DateTime
  createdAt DateTime @default(now())
}
```

- [ ] **Step 3: 마이그레이션 실행**

```bash
cd /opt/real-es && pnpm prisma migrate dev --name init_auth
```
Expected: `prisma/migrations/<ts>_init_auth/` 생성, `Your database is now in sync with your schema.`, Prisma Client 재생성.

- [ ] **Step 4: 검증 — 마이그레이션 상태 + 테이블**

```bash
cd /opt/real-es && pnpm prisma migrate status
docker exec mrms-pg psql -U mrms -d real_es -tc "\dt" | grep -E "User|Session"
```
Expected: `Database schema is up to date!`, `User`·`Session` 테이블 출력.

- [ ] **Step 5: Commit**

```bash
cd /opt/real-es
git add prisma package.json pnpm-lock.yaml
git commit -m "feat(auth): User/Session 스키마 + init_auth 마이그레이션, bcryptjs·zod 추가"
```

---

## Task 2: vitest 셋업 + password 모듈 (TDD)

**Files:**
- Modify: `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`
- Create: `vitest.config.ts`, `vitest.setup.ts`
- Create: `src/lib/auth/password.ts`, `src/lib/auth/password.test.ts`

**Interfaces:**
- Produces:
  - `hashPassword(password: string): Promise<string>`
  - `verifyPassword(password: string, hash: string): Promise<boolean>`
  - `pnpm test:unit` 명령 (vitest, `.env` 로드, `@/` alias 해석)

- [ ] **Step 1: vitest 의존성 설치 + esbuild build 승인**

```bash
cd /opt/real-es
pnpm add -D vitest vite-tsconfig-paths dotenv
```
> vitest는 esbuild(네이티브 바이너리)를 끌어와 pnpm이 build script를 막는다. `pnpm-workspace.yaml`의 `allowBuilds`에 `esbuild: true`를 추가한다 (단계 0의 prisma 승인과 동일 패턴):

`pnpm-workspace.yaml`을 다음으로 만든다(기존 항목 보존 + esbuild 추가):
```yaml
allowBuilds:
  esbuild: true
  msw: true
  sharp: true
  unrs-resolver: true
```
그 후 build script 실행:
```bash
cd /opt/real-es && pnpm install
```
Expected: esbuild postinstall 실행됨, `ERR_PNPM_IGNORED_BUILDS` 사라짐.

- [ ] **Step 2: vitest 설정 파일 작성**

`vitest.config.ts`:
```typescript
import { defineConfig } from "vitest/config";
import tsconfigPaths from "vite-tsconfig-paths";

export default defineConfig({
  plugins: [tsconfigPaths()],
  test: {
    environment: "node",
    setupFiles: ["./vitest.setup.ts"],
    include: ["src/**/*.test.ts"],
  },
});
```

`vitest.setup.ts` (테스트에서 `DATABASE_URL` 등 `.env` 로드):
```typescript
import { config } from "dotenv";

config();
```

`package.json`의 `scripts`에 추가 (기존 스크립트 유지):
```json
"test:unit": "vitest run"
```

- [ ] **Step 3: 실패하는 password 테스트 작성**

`src/lib/auth/password.test.ts`:
```typescript
import { describe, expect, it } from "vitest";

import { hashPassword, verifyPassword } from "./password";

describe("password", () => {
  it("hashes and verifies the correct password", async () => {
    const hash = await hashPassword("correct-horse-battery");
    expect(hash).not.toBe("correct-horse-battery");
    expect(await verifyPassword("correct-horse-battery", hash)).toBe(true);
  });

  it("rejects an incorrect password", async () => {
    const hash = await hashPassword("correct-horse-battery");
    expect(await verifyPassword("wrong-password", hash)).toBe(false);
  });
});
```

- [ ] **Step 4: 테스트 실행 → 실패 확인**

```bash
cd /opt/real-es && pnpm test:unit src/lib/auth/password.test.ts
```
Expected: FAIL — `./password` 모듈 없음(import 에러).

- [ ] **Step 5: password 모듈 구현**

`src/lib/auth/password.ts`:
```typescript
import bcrypt from "bcryptjs";

const SALT_ROUNDS = 12;

export function hashPassword(password: string): Promise<string> {
  return bcrypt.hash(password, SALT_ROUNDS);
}

export function verifyPassword(password: string, hash: string): Promise<boolean> {
  return bcrypt.compare(password, hash);
}
```

- [ ] **Step 6: 테스트 실행 → 통과 확인**

```bash
cd /opt/real-es && pnpm test:unit src/lib/auth/password.test.ts
```
Expected: PASS (2 tests).

- [ ] **Step 7: Commit**

```bash
cd /opt/real-es
git add package.json pnpm-lock.yaml pnpm-workspace.yaml vitest.config.ts vitest.setup.ts src/lib/auth/password.ts src/lib/auth/password.test.ts
git commit -m "feat(auth): vitest 셋업 + bcryptjs 비밀번호 해싱 모듈"
```

---

## Task 3: session 모듈 (TDD, real_es 통합)

**Files:**
- Create: `src/lib/auth/session.ts`, `src/lib/auth/session.test.ts`

**Interfaces:**
- Consumes: `db` (`src/lib/db.ts`), `User`/`Session` (Task 1)
- Produces:
  - `createSession(userId: string): Promise<{ token: string; expiresAt: Date }>`
  - `validateSessionToken(token: string): Promise<{ user: User; session: Session } | null>`
  - `invalidateSession(token: string): Promise<void>`

- [ ] **Step 1: 실패하는 session 테스트 작성**

`src/lib/auth/session.test.ts`:
```typescript
import { afterAll, beforeAll, beforeEach, describe, expect, it } from "vitest";

import { db } from "@/lib/db";
import { createSession, invalidateSession, validateSessionToken } from "./session";

let agencyId: string;
let userId: string;

beforeAll(async () => {
  const agency = await db.agency.create({ data: { name: "세션테스트부동산" } });
  agencyId = agency.id;
  const user = await db.user.create({
    data: { agencyId, email: `session-${Date.now()}@example.com`, passwordHash: "x", role: "admin" },
  });
  userId = user.id;
});

beforeEach(async () => {
  await db.session.deleteMany({ where: { userId } });
});

afterAll(async () => {
  await db.session.deleteMany({ where: { userId } });
  await db.user.delete({ where: { id: userId } });
  await db.agency.delete({ where: { id: agencyId } });
  await db.$disconnect();
});

describe("session", () => {
  it("creates a session and validates its token to the user", async () => {
    const { token } = await createSession(userId);
    const result = await validateSessionToken(token);
    expect(result?.user.id).toBe(userId);
  });

  it("returns null for an expired session", async () => {
    const { token } = await createSession(userId);
    await db.session.updateMany({
      where: { userId },
      data: { expiresAt: new Date(Date.now() - 1000) },
    });
    expect(await validateSessionToken(token)).toBeNull();
  });

  it("returns null after the session is invalidated", async () => {
    const { token } = await createSession(userId);
    await invalidateSession(token);
    expect(await validateSessionToken(token)).toBeNull();
  });

  it("returns null for an unknown token", async () => {
    expect(await validateSessionToken("not-a-real-token")).toBeNull();
  });
});
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

```bash
cd /opt/real-es && pnpm test:unit src/lib/auth/session.test.ts
```
Expected: FAIL — `./session` 모듈 없음.

- [ ] **Step 3: session 모듈 구현**

`src/lib/auth/session.ts`:
```typescript
import { createHash, randomBytes } from "crypto";

import type { Session, User } from "@prisma/client";

import { db } from "@/lib/db";

const SESSION_TTL_MS = 1000 * 60 * 60 * 24 * 30; // 30일

function hashToken(token: string): string {
  return createHash("sha256").update(token).digest("hex");
}

export async function createSession(userId: string): Promise<{ token: string; expiresAt: Date }> {
  const token = randomBytes(32).toString("base64url");
  const expiresAt = new Date(Date.now() + SESSION_TTL_MS);
  await db.session.create({
    data: { userId, tokenHash: hashToken(token), expiresAt },
  });
  return { token, expiresAt };
}

export async function validateSessionToken(
  token: string,
): Promise<{ user: User; session: Session } | null> {
  const found = await db.session.findUnique({
    where: { tokenHash: hashToken(token) },
    include: { user: true },
  });
  if (!found) return null;
  if (found.expiresAt.getTime() < Date.now()) {
    await db.session.delete({ where: { id: found.id } }).catch(() => {});
    return null;
  }
  const { user, ...session } = found;
  return { user, session };
}

export async function invalidateSession(token: string): Promise<void> {
  await db.session.deleteMany({ where: { tokenHash: hashToken(token) } });
}
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

```bash
cd /opt/real-es && pnpm test:unit src/lib/auth/session.test.ts
```
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
cd /opt/real-es
git add src/lib/auth/session.ts src/lib/auth/session.test.ts
git commit -m "feat(auth): opaque 토큰 세션 모듈(생성/검증/만료/무효화)"
```

---

## Task 4: 쿠키·현재 유저 헬퍼 + 서버 액션

**Files:**
- Create: `src/lib/auth/cookies.ts`, `src/lib/auth/current-user.ts`
- Create: `src/app/(auth)/actions.ts`

**Interfaces:**
- Consumes: `hashPassword`/`verifyPassword`(Task 2), `createSession`/`validateSessionToken`/`invalidateSession`(Task 3), `db`
- Produces:
  - `setSessionCookie(token, expiresAt)` · `clearSessionCookie()` · `getSessionToken(): Promise<string | undefined>`
  - `getCurrentUser(): Promise<User | null>`
  - `signupAction(prev, formData)` · `loginAction(prev, formData)` · `logoutAction()`; `type AuthState = { error: string | null }`

- [ ] **Step 1: 쿠키 헬퍼 작성**

`src/lib/auth/cookies.ts` (Next 16 — `cookies()`는 async):
```typescript
import { cookies } from "next/headers";

const COOKIE_NAME = "session";

export async function setSessionCookie(token: string, expiresAt: Date): Promise<void> {
  const store = await cookies();
  store.set(COOKIE_NAME, token, {
    httpOnly: true,
    secure: process.env.NODE_ENV === "production",
    sameSite: "lax",
    path: "/",
    expires: expiresAt,
  });
}

export async function clearSessionCookie(): Promise<void> {
  const store = await cookies();
  store.delete(COOKIE_NAME);
}

export async function getSessionToken(): Promise<string | undefined> {
  const store = await cookies();
  return store.get(COOKIE_NAME)?.value;
}
```

- [ ] **Step 2: 현재 유저 헬퍼 작성**

`src/lib/auth/current-user.ts`:
```typescript
import type { User } from "@prisma/client";

import { getSessionToken } from "./cookies";
import { validateSessionToken } from "./session";

export async function getCurrentUser(): Promise<User | null> {
  const token = await getSessionToken();
  if (!token) return null;
  const result = await validateSessionToken(token);
  return result?.user ?? null;
}
```

- [ ] **Step 3: 서버 액션 작성**

`src/app/(auth)/actions.ts`:
```typescript
"use server";

import { redirect } from "next/navigation";
import { z } from "zod";

import { db } from "@/lib/db";
import { clearSessionCookie, getSessionToken, setSessionCookie } from "@/lib/auth/cookies";
import { hashPassword, verifyPassword } from "@/lib/auth/password";
import { createSession, invalidateSession } from "@/lib/auth/session";

export type AuthState = { error: string | null };

const DASHBOARD = "/dashboard/real-estate";

const signupSchema = z.object({
  agencyName: z.string().min(1, "상호명을 입력하세요"),
  name: z.string().trim().optional(),
  email: z.string().email("올바른 이메일을 입력하세요"),
  password: z.string().min(8, "비밀번호는 8자 이상이어야 합니다"),
});

export async function signupAction(_prev: AuthState, formData: FormData): Promise<AuthState> {
  const parsed = signupSchema.safeParse({
    agencyName: formData.get("agencyName"),
    name: formData.get("name") || undefined,
    email: formData.get("email"),
    password: formData.get("password"),
  });
  if (!parsed.success) {
    return { error: parsed.error.issues[0].message };
  }
  const { agencyName, name, email, password } = parsed.data;

  if (await db.user.findUnique({ where: { email } })) {
    return { error: "이미 가입된 이메일입니다" };
  }

  const passwordHash = await hashPassword(password);
  const user = await db.$transaction(async (tx) => {
    const agency = await tx.agency.create({ data: { name: agencyName } });
    return tx.user.create({
      data: { agencyId: agency.id, email, passwordHash, name: name ?? null, role: "admin" },
    });
  });

  const { token, expiresAt } = await createSession(user.id);
  await setSessionCookie(token, expiresAt);
  redirect(DASHBOARD);
}

const loginSchema = z.object({
  email: z.string().email("올바른 이메일을 입력하세요"),
  password: z.string().min(1, "비밀번호를 입력하세요"),
});

export async function loginAction(_prev: AuthState, formData: FormData): Promise<AuthState> {
  const parsed = loginSchema.safeParse({
    email: formData.get("email"),
    password: formData.get("password"),
  });
  if (!parsed.success) {
    return { error: parsed.error.issues[0].message };
  }
  const { email, password } = parsed.data;

  const user = await db.user.findUnique({ where: { email } });
  if (!user || !(await verifyPassword(password, user.passwordHash))) {
    return { error: "이메일 또는 비밀번호가 올바르지 않습니다" };
  }

  const { token, expiresAt } = await createSession(user.id);
  await setSessionCookie(token, expiresAt);
  redirect(DASHBOARD);
}

export async function logoutAction(): Promise<void> {
  const token = await getSessionToken();
  if (token) await invalidateSession(token);
  await clearSessionCookie();
  redirect("/login");
}
```

- [ ] **Step 4: 타입체크로 검증**

```bash
cd /opt/real-es && pnpm exec tsc --noEmit
```
Expected: 에러 없음 (exit 0).

- [ ] **Step 5: Commit**

```bash
cd /opt/real-es
git add src/lib/auth/cookies.ts src/lib/auth/current-user.ts "src/app/(auth)/actions.ts"
git commit -m "feat(auth): 세션 쿠키·현재 유저 헬퍼 + 회원가입/로그인/로그아웃 서버 액션"
```

---

## Task 5: UI 연결 + 보호 라우트

**Files:**
- Modify: `src/app/(auth)/login/page.tsx`, `src/app/(auth)/register/page.tsx`, `src/app/(auth)/layout.tsx`, `src/app/(dashboard)/layout.tsx`, `src/components/layout/app-header.tsx`
- Create: `src/components/auth/logout-button.tsx`

**Interfaces:**
- Consumes: `signupAction`/`loginAction`/`logoutAction`/`AuthState`(Task 4), `getCurrentUser`(Task 4)
- Produces: 동작하는 로그인/회원가입 폼, 보호된 대시보드, 헤더 로그아웃 버튼

- [ ] **Step 1: 로그인 페이지를 서버 액션에 연결**

`src/app/(auth)/login/page.tsx` 전체를 교체:
```tsx
"use client"

import { useActionState, useState } from "react"
import Link from "next/link"
import { Eye, EyeOff } from "lucide-react"

import { loginAction, type AuthState } from "../actions"
import { AuthCard } from "@/components/auth/auth-card"
import { SocialButtons } from "@/components/auth/social-buttons"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
import { Separator } from "@/components/ui/separator"

const initialState: AuthState = { error: null }

export default function LoginPage() {
  const [showPassword, setShowPassword] = useState(false)
  const [state, formAction, pending] = useActionState(loginAction, initialState)

  return (
    <AuthCard
      title="Welcome back"
      footer={
        <span className="text-muted-foreground">
          Don&apos;t have an account?{" "}
          <Link href="/register" className="text-foreground underline-offset-4 hover:underline font-medium">
            Sign up
          </Link>
        </span>
      }
    >
      <form action={formAction} className="flex flex-col gap-4">
        <div className="flex flex-col gap-1.5">
          <Label htmlFor="email">Email</Label>
          <Input id="email" name="email" type="email" placeholder="m@example.com" autoComplete="email" />
        </div>

        <div className="flex flex-col gap-1.5">
          <div className="flex items-center justify-between">
            <Label htmlFor="password">Password</Label>
            <Link
              href="/forgot-password"
              className="text-xs text-muted-foreground underline-offset-4 hover:underline"
            >
              Forgot password?
            </Link>
          </div>
          <div className="relative">
            <Input
              id="password"
              name="password"
              type={showPassword ? "text" : "password"}
              autoComplete="current-password"
              className="pr-9"
            />
            <button
              type="button"
              onClick={() => setShowPassword((v) => !v)}
              className="absolute right-2.5 top-1/2 -translate-y-1/2 text-muted-foreground hover:text-foreground transition-colors"
              aria-label={showPassword ? "Hide password" : "Show password"}
            >
              {showPassword ? <EyeOff className="size-4" /> : <Eye className="size-4" />}
            </button>
          </div>
        </div>

        {state.error && <p className="text-sm text-destructive">{state.error}</p>}

        <Button type="submit" disabled={pending} className="w-full mt-1">
          {pending ? "처리 중..." : "Sign in"}
        </Button>

        <div className="flex items-center gap-3 my-1">
          <Separator className="flex-1" />
          <span className="text-xs text-muted-foreground shrink-0">Or continue with</span>
          <Separator className="flex-1" />
        </div>

        <SocialButtons />
      </form>
    </AuthCard>
  )
}
```
> 변경점: `Checkbox`/`rememberMe`/`handleSubmit` 제거, `useActionState`로 `loginAction` 연결, email·password에 `name` 추가, 에러 표시, pending 시 버튼 비활성화.

- [ ] **Step 2: 회원가입 페이지에 상호명 필드 추가 + 서버 액션 연결**

`src/app/(auth)/register/page.tsx` 전체를 교체:
```tsx
"use client"

import { useActionState } from "react"
import Link from "next/link"

import { signupAction, type AuthState } from "../actions"
import { AuthCard } from "@/components/auth/auth-card"
import { SocialButtons } from "@/components/auth/social-buttons"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
import { Separator } from "@/components/ui/separator"

const initialState: AuthState = { error: null }

export default function RegisterPage() {
  const [state, formAction, pending] = useActionState(signupAction, initialState)

  return (
    <AuthCard
      title="Create an account"
      footer={
        <span className="text-muted-foreground">
          Already have an account?{" "}
          <Link href="/login" className="text-foreground underline-offset-4 hover:underline font-medium">
            Sign in
          </Link>
        </span>
      }
    >
      <form action={formAction} className="flex flex-col gap-4">
        <div className="flex flex-col gap-1.5">
          <Label htmlFor="agencyName">상호명</Label>
          <Input id="agencyName" name="agencyName" type="text" placeholder="○○공인중개사" autoComplete="organization" />
        </div>

        <div className="flex flex-col gap-1.5">
          <Label htmlFor="name">이름 (선택)</Label>
          <Input id="name" name="name" type="text" placeholder="홍길동" autoComplete="name" />
        </div>

        <div className="flex flex-col gap-1.5">
          <Label htmlFor="email">Email</Label>
          <Input id="email" name="email" type="email" placeholder="m@example.com" autoComplete="email" />
        </div>

        <div className="flex flex-col gap-1.5">
          <Label htmlFor="password">Password</Label>
          <Input id="password" name="password" type="password" autoComplete="new-password" />
        </div>

        {state.error && <p className="text-sm text-destructive">{state.error}</p>}

        <Button type="submit" disabled={pending} className="w-full mt-1">
          {pending ? "처리 중..." : "Create account"}
        </Button>

        <div className="flex items-center gap-3 my-1">
          <Separator className="flex-1" />
          <span className="text-xs text-muted-foreground shrink-0">Or continue with</span>
          <Separator className="flex-1" />
        </div>

        <SocialButtons />
      </form>
    </AuthCard>
  )
}
```
> 변경점: `agreed`/`Checkbox`/terms 제거, 상호명(`agencyName`)·이름(`name`)·email·password 필드에 `name` 부여, `useActionState`로 `signupAction` 연결.

- [ ] **Step 3: 로그아웃 버튼 컴포넌트 생성**

`src/components/auth/logout-button.tsx`:
```tsx
"use client"

import { logoutAction } from "@/app/(auth)/actions"
import { Button } from "@/components/ui/button"

export function LogoutButton() {
  return (
    <form action={logoutAction}>
      <Button type="submit" variant="ghost" size="sm">
        로그아웃
      </Button>
    </form>
  )
}
```

- [ ] **Step 4: 헤더에 로그아웃 버튼 추가**

`src/components/layout/app-header.tsx`에서 `Avatar` import 아래에 추가:
```tsx
import { LogoutButton } from "@/components/auth/logout-button";
```
그리고 `ml-auto` div의 `Avatar` 바로 다음에 `<LogoutButton />`을 추가:
```tsx
        <Avatar className="size-8">
          <AvatarFallback>TB</AvatarFallback>
        </Avatar>
        <LogoutButton />
```

- [ ] **Step 5: 대시보드 레이아웃에 인증 가드 추가**

`src/app/(dashboard)/layout.tsx` 전체를 교체:
```tsx
import { redirect } from "next/navigation";

import { SidebarInset, SidebarProvider } from "@/components/ui/sidebar";
import { AppSidebar } from "@/components/layout/app-sidebar";
import { AppHeader } from "@/components/layout/app-header";
import { getCurrentUser } from "@/lib/auth/current-user";

export default async function DashboardLayout({ children }: { children: React.ReactNode }) {
  const user = await getCurrentUser();
  if (!user) redirect("/login");

  return (
    <SidebarProvider>
      <AppSidebar />
      <SidebarInset>
        <AppHeader />
        <main className="flex flex-1 flex-col gap-4 p-4">{children}</main>
      </SidebarInset>
    </SidebarProvider>
  );
}
```

- [ ] **Step 6: 인증 레이아웃에 로그인 상태 리다이렉트 추가**

`src/app/(auth)/layout.tsx` 전체를 교체:
```tsx
import { redirect } from "next/navigation";

import { getCurrentUser } from "@/lib/auth/current-user";

export default async function AuthLayout({ children }: { children: React.ReactNode }) {
  const user = await getCurrentUser();
  if (user) redirect("/dashboard/real-estate");

  return (
    <div className="flex min-h-svh items-center justify-center bg-muted/30 p-4">
      <div className="w-full max-w-sm">{children}</div>
    </div>
  );
}
```

- [ ] **Step 7: 프로덕션 빌드로 검증**

```bash
cd /opt/real-es && pnpm build
```
Expected: 빌드 성공(타입체크 통과, 컴파일 완료). `(dashboard)`·`(auth)` 라우트가 cookies 사용으로 dynamic 처리됨.

> ⚠️ 이 시점부터 가드가 미인증 `/dashboard/*` 접근을 `/login`으로 보낸다. 기존 템플릿 e2e(components-*, dashboards-* 등)는 인증 없이 `/dashboard/*`를 방문하므로 **Task 6의 인증 픽스처를 넣기 전까지 전체 e2e 스위트는 빨갛다.** 그래서 이 Task의 게이트는 전체 e2e가 아니라 `pnpm build`다. 전체 스위트는 Task 6에서 초록으로 만든다.

- [ ] **Step 8: Commit**

```bash
cd /opt/real-es
git add "src/app/(auth)/login/page.tsx" "src/app/(auth)/register/page.tsx" "src/app/(auth)/layout.tsx" "src/app/(dashboard)/layout.tsx" src/components/layout/app-header.tsx src/components/auth/logout-button.tsx
git commit -m "feat(auth): 로그인/회원가입 폼 연결 + 대시보드 인증 가드 + 로그아웃 버튼"
```

---

## Task 6: 인증 E2E + 기존 스위트 인증 픽스처

가드 도입으로 깨진 기존 템플릿 e2e를 storageState로 살리고, 인증 플로우 자체를 검증한다.

**Files:**
- Create: `e2e/global.setup.ts`, `e2e/auth.spec.ts`
- Modify: `playwright.config.ts`, `.gitignore`

**Interfaces:**
- Consumes: 전체 인증 플로우(Task 5까지)
- Produces: 인증된 storageState(`playwright/.auth/user.json`)로 기존 대시보드 테스트 통과 + 인증 E2E 통과 → **로그인되는 앱** 검증

- [ ] **Step 1: `.gitignore`에 auth 상태 디렉토리 추가**

`.gitignore` 끝에 추가:
```
# Playwright 인증 storageState
/playwright/.auth/
```

- [ ] **Step 2: 인증 setup 파일 작성 (유저 등록 → storageState 저장)**

`e2e/global.setup.ts`:
```typescript
import { test as setup } from "@playwright/test";

const authFile = "playwright/.auth/user.json";

setup("authenticate", async ({ page }) => {
  const email = `setup-${Date.now()}@example.com`;
  await page.goto("/register");
  await page.getByLabel("상호명").fill("Setup공인중개사");
  await page.getByLabel("Email").fill(email);
  await page.getByLabel("Password").fill("setup-password-123");
  await page.getByRole("button", { name: "Create account" }).click();
  await page.waitForURL(/\/dashboard\/real-estate/);
  await page.context().storageState({ path: authFile });
});
```

- [ ] **Step 3: `playwright.config.ts`를 프로젝트 분리로 교체**

```typescript
import { defineConfig, devices } from "@playwright/test";

export default defineConfig({
  testDir: "./e2e",
  fullyParallel: true,
  reporter: "list",
  use: { baseURL: "http://localhost:3001", trace: "on-first-retry" },
  projects: [
    { name: "setup", testMatch: /global\.setup\.ts/ },
    {
      name: "auth",
      testMatch: /auth\.spec\.ts/,
      use: { ...devices["Desktop Chrome"] }, // 미인증 컨텍스트
      dependencies: ["setup"],
    },
    {
      name: "chromium",
      testIgnore: [/auth\.spec\.ts/, /global\.setup\.ts/],
      use: { ...devices["Desktop Chrome"], storageState: "playwright/.auth/user.json" },
      dependencies: ["setup"],
    },
  ],
  webServer: {
    command: "pnpm build && pnpm start",
    url: "http://localhost:3001",
    reuseExistingServer: !process.env.CI,
    timeout: 180_000,
  },
});
```
> `setup`이 유저를 등록해 storageState를 저장 → `chromium`(기존 템플릿 테스트)은 인증 쿠키를 갖고 실행돼 가드를 통과. `auth`(인증 플로우 테스트)는 storageState 없이 실행.

- [ ] **Step 4: 인증 E2E 작성**

`e2e/auth.spec.ts`:
```typescript
import { test, expect } from "@playwright/test";

test("unauthenticated dashboard access redirects to login", async ({ page }) => {
  await page.goto("/dashboard/real-estate");
  await expect(page).toHaveURL(/\/login/);
});

test("register, log out, and log back in", async ({ page }) => {
  const email = `e2e-${Date.now()}@example.com`;
  const password = "test-password-123";

  // 회원가입 → 대시보드
  await page.goto("/register");
  await page.getByLabel("상호명").fill("E2E공인중개사");
  await page.getByLabel("Email").fill(email);
  await page.getByLabel("Password").fill(password);
  await page.getByRole("button", { name: "Create account" }).click();
  await expect(page).toHaveURL(/\/dashboard\/real-estate/);

  // 로그아웃 → 로그인 페이지
  await page.getByRole("button", { name: "로그아웃" }).click();
  await expect(page).toHaveURL(/\/login/);

  // 로그인 → 대시보드
  await page.getByLabel("Email").fill(email);
  await page.getByLabel("Password").fill(password);
  await page.getByRole("button", { name: "Sign in" }).click();
  await expect(page).toHaveURL(/\/dashboard\/real-estate/);
});

test("login with wrong password shows an error", async ({ page }) => {
  await page.goto("/login");
  await page.getByLabel("Email").fill("nobody@example.com");
  await page.getByLabel("Password").fill("definitely-wrong");
  await page.getByRole("button", { name: "Sign in" }).click();
  await expect(page.getByText("이메일 또는 비밀번호가 올바르지 않습니다")).toBeVisible();
});
```
> 참고: 테스트는 `real_es`에 실제 Agency/User를 만든다(이메일에 타임스탬프로 충돌 회피). dev DB라 누적은 허용. CI는 아직 없음.

- [ ] **Step 5: 인증 E2E만 먼저 실행 → 통과 확인**

```bash
cd /opt/real-es && pnpm exec playwright test --project=auth
```
Expected: PASS (3 tests).

- [ ] **Step 6: 전체 스위트 실행 → 초록 확인 (기존 테스트가 인증 픽스처로 통과)**

```bash
cd /opt/real-es && pnpm exec playwright test
```
Expected: 전체 통과(setup 1 + auth 3 + 기존 템플릿 테스트). 단계 0에서 관찰된 hover flaky(components-calendar/hover-card/tooltip) 3건은 부하 시 재현될 수 있으니, 실패하면 `--retries=2`로 재확인(본 변경과 무관).

- [ ] **Step 7: Commit**

```bash
cd /opt/real-es
git add e2e/global.setup.ts e2e/auth.spec.ts playwright.config.ts .gitignore
git commit -m "test(auth): 인증 E2E + storageState 픽스처로 기존 대시보드 스위트 인증"
```

---

## Task 7: 문서 갱신

**Files:**
- Modify: `README.md`, `docs/PROJECT_GUIDE.md`

- [ ] **Step 1: README `현재 반영 상태`에 한 줄 추가**

`README.md`의 `## 현재 반영 상태` 마지막 누적 항목 다음에:
```markdown
- 인증 단계 1(로그인되는 앱): `User`/`Session` 모델 + `init_auth` 마이그레이션. 셀프서비스 회원가입(Agency+admin 동시 생성)·로그인·로그아웃, opaque 토큰 세션(httpOnly 쿠키·DB tokenHash). `(dashboard)` 서버 사이드 가드로 보호. 인증 E2E(`e2e/auth.spec.ts`) 통과.
```

- [ ] **Step 2: PROJECT_GUIDE `현재 참고 상태`에 한 줄 추가**

`docs/PROJECT_GUIDE.md`의 `## 8. 현재 참고 상태`에:
```markdown
- **인증 단계 1 완료**: `src/lib/auth/`(password·session·cookies·current-user) + `(auth)/actions.ts` 서버 액션. 회원가입이 Agency+admin User 생성, 세션은 opaque 토큰+DB tokenHash(30일). 보호는 `(dashboard)/layout.tsx` 가드. 다음 단계 2(스크래퍼)는 이 인증 위에서 진행. 계획: `docs/superpowers/plans/2026-06-23-stage-1-auth.md`.
```

- [ ] **Step 3: Commit**

```bash
cd /opt/real-es
git add README.md docs/PROJECT_GUIDE.md
git commit -m "docs: 단계 1 인증 반영 상태 기록"
```

---

## 단계 1 완료 기준 (Definition of Done)

- `pnpm prisma migrate status` up to date, `real_es`에 `User`·`Session` 테이블 존재
- `pnpm test:unit` 통과 (password 2 + session 4)
- `pnpm build` 성공
- `pnpm exec playwright test --project=auth` 통과 → 회원가입·로그아웃·로그인·보호 라우트 동작 = **로그인되는 앱**
- `pnpm exec playwright test` 전체 통과 — 기존 템플릿 대시보드 테스트가 storageState 인증 픽스처로 가드를 통과(가드 도입 회귀 없음)
- 다음 단계 2(스크래퍼 모듈)에서 이 인증/테넌트 위에 `searchComplexes`/`getComplexDetail`를 얹는다

---

## Self-Review

**1. Spec coverage:** 설계 스펙 §2 범위(회원가입 셀프서비스·로그인·로그아웃·보호 라우트) → Task 4(액션)·Task 5(UI/가드). §3 데이터 모델(User/Session, tokenHash 델타) → Task 1. §4 인증 코어(password/session/cookies/current-user) → Task 2·3·4. §5 흐름 → Task 4 액션. §6 보호 라우트 → Task 5. §7 에러 처리 → Task 4 액션 + Task 5 표시. §8 테스트(단위/통합/E2E) → Task 2·3(vitest)·Task 6(Playwright). §9 deps(bcryptjs/zod) → Task 1. 비범위(재설정/인증메일)는 미연결 유지. **가드 부작용**(기존 템플릿 e2e가 가드에 막힘) → Task 6의 storageState 인증 픽스처로 해소. ✓

**2. Placeholder scan:** TBD/TODO 없음. 모든 파일에 완전한 코드·명령·기대 출력 명시. ✓

**3. Type consistency:** `AuthState`(Task 4) → Task 5 페이지에서 동일 import. `getCurrentUser`(Task 4) → Task 5 두 레이아웃에서 동일 사용. `createSession`/`validateSessionToken`/`invalidateSession` 시그니처(Task 3) → Task 4 액션·current-user에서 동일. `signupAction`/`loginAction`/`logoutAction`(Task 4) → Task 5 폼·로그아웃 버튼에서 동일. Prisma `User`/`Session` 필드(Task 1) ↔ session.ts·actions.ts 사용 일치. ✓
