# 일정관리(캘린더) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `/dashboard/calendar` 월 캘린더에서 매물 일정(계약일·잔금일·입주일 등)을 자동 표시하고, 수기 일정(미팅·임장 등)을 DB로 CRUD한다.

**Architecture:** 신규 `Event` 모델 + 순수 모듈(카테고리·월그리드·매물파생) + 서버 액션(loadCalendar+CRUD) + 클라 뷰(월그리드·다이얼로그). 매물 파생 일정은 매물 날짜에서 계산(읽기전용).

**Tech Stack:** Next.js 16 App Router, React 19, Prisma 6/PostgreSQL, Tailwind v4, Base UI(dialog·native-select), vitest, pnpm. 설계: [docs/superpowers/specs/2026-06-26-calendar-design.md](../specs/2026-06-26-calendar-design.md).

## Global Constraints

- 응답·UI 카피·주석 **한국어**, 식별자 영문. 템플릿/기존 패턴 우선, 임의 px/hex 금지(색은 Tailwind 토큰 클래스).
- DB 변경은 **비파괴 마이그레이션**. `db` 싱글톤(`@/lib/db`). 보호 라우트·액션은 `getCurrentUser()` userId 스코프(IDOR 차단). `propertyId`/`customerId`는 소유 검증.
- month는 **0-indexed**(0=1월, JS Date와 동일)로 통일. `Event.date`·매물 날짜는 **YYYYMMDD 문자열**.
- 검증: 순수 `pnpm exec vitest run <file>`, 타입 `pnpm exec tsc --noEmit`, 린트 `pnpm exec eslint <files>`, 빌드 `pnpm build`("use server" async-export·RSC 직렬화는 build/런타임만).
- ⛔ 서브에이전트: `git restore/checkout/clean/stash/reset` 금지, `git add`는 명시 파일만. `docs/project_structure.md`·`매물_샘플_*.xlsx`·`.env`·README/PROJECT_GUIDE 손대지 말 것(README/GUIDE는 컨트롤러가 마감 일괄). **CLAUDE.md §5 README/GUIDE per-task 수정 금지.**

## File Structure

| 파일 | 책임 | 태스크 |
|---|---|---|
| `prisma/schema.prisma` + 마이그레이션 | `Event` 모델 + Property/Customer/User 역참조 | 1 |
| `src/lib/nav.ts` | "일정관리" 메뉴 | 1 |
| `src/lib/calendar/categories.ts` | 일정 유형·색 단일정의 | 2 |
| `src/lib/calendar/month-grid.ts` (+test) | 월 그리드·한국어 명칭·ymd (순수) | 2 |
| `src/lib/calendar/property-events.ts` (+test) | 매물 날짜 → 파생 일정 (순수) | 3 |
| `.../dashboard/calendar/actions.ts` | loadCalendar + CRUD | 4 |
| `.../dashboard/calendar/page.tsx`·`calendar-view.tsx` | 페이지·월그리드·사이드바 | 5 |
| `.../dashboard/calendar/event-dialog.tsx` | 일정 추가/수정/삭제 다이얼로그 | 6 |

---

## Task 1: 스키마·역참조·메뉴

**Files:**
- Modify: `prisma/schema.prisma`, `src/lib/nav.ts`

**Interfaces:**
- Produces: `Event` 모델(`db.event`), Property/Customer/User `events` 역참조, nav "일정관리".

- [ ] **Step 1: schema에 Event 추가**

`prisma/schema.prisma` 끝에 추가:
```prisma
model Event {
  id         String    @id @default(cuid())
  userId     String
  user       User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  title      String
  date       String    // YYYYMMDD
  startTime  String?   // "HH:MM" 또는 null=종일
  category   String    @default("기타") // 미팅 | 임장 | 계약 | 기타
  memo       String?
  propertyId String?
  property   Property? @relation(fields: [propertyId], references: [id], onDelete: SetNull)
  customerId String?
  customer   Customer? @relation(fields: [customerId], references: [id], onDelete: SetNull)
  createdAt  DateTime  @default(now())
  updatedAt  DateTime  @updatedAt

  @@index([userId, date])
}
```

- [ ] **Step 2: 역참조 추가**

`model User { ... }` 의 `customers Customer[]` 줄 다음에:
```prisma
  events       Event[]
```
`model Property { ... }` 의 `customers Customer[]` 줄 다음에:
```prisma
  events    Event[]
```
`model Customer { ... }` 의 `propertyId String?` 줄 **앞**(또는 `@@index` 위 아무 곳)에:
```prisma
  events     Event[]
```
(Prisma가 역참조 누락 시 `migrate dev`에서 에러로 알려줌 — 그러면 해당 모델에 `events Event[]` 추가.)

- [ ] **Step 3: 마이그레이션**

Run: `pnpm exec prisma migrate dev --name add_event`
Expected: `CREATE TABLE "Event"` + FK·인덱스만(비파괴), prisma generate.

- [ ] **Step 4: nav 메뉴 추가**

`src/lib/nav.ts`의 `navGroups[0].items`(매물 관리 그룹)에서 "고객관리" 줄 다음에 추가(`Calendar`는 이미 import됨):
```ts
      { title: "일정관리", href: "/dashboard/calendar", icon: Calendar },
```

- [ ] **Step 5: 검증·커밋**

Run: `pnpm exec tsc --noEmit`
Expected: 에러 없음.
```bash
git add prisma/schema.prisma prisma/migrations src/lib/nav.ts
git commit -m "feat(calendar): Event 모델·역참조·메뉴 추가"
```

---

## Task 2: 카테고리·월그리드 (순수, TDD)

**Files:**
- Create: `src/lib/calendar/categories.ts`, `src/lib/calendar/month-grid.ts`, `src/lib/calendar/month-grid.test.ts`

**Interfaces:**
- Produces:
  - `categories.ts`: `type CalendarCategory = "미팅"|"임장"|"계약"|"기타"`; `CALENDAR_CATEGORIES: { value: CalendarCategory; dot: string; text: string }[]`; `PROPERTY_EVENT_STYLE: { dot: string; text: string }`; `categoryStyle(value: string): { dot: string; text: string }`
  - `month-grid.ts`: `MONTH_NAMES_KO: string[]`(1월~12월); `WEEKDAYS_KO: string[]`(일~토); `type GridDay = { year: number; month: number; day: number; isCurrentMonth: boolean }`; `buildMonthGrid(year: number, month: number): GridDay[]`(month 0-indexed, 42칸); `ymd(year: number, month: number, day: number): string`(YYYYMMDD, month 0-indexed→+1); `monthPrefix(year: number, month: number): string`(YYYYMM)

- [ ] **Step 1: 실패 테스트**

`src/lib/calendar/month-grid.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { buildMonthGrid, ymd, monthPrefix, MONTH_NAMES_KO, WEEKDAYS_KO } from "./month-grid";

describe("buildMonthGrid (2026년 6월=month 5)", () => {
  const grid = buildMonthGrid(2026, 5);
  it("항상 42칸", () => {
    expect(grid).toHaveLength(42);
  });
  it("6월 1일은 월요일(인덱스 1) — 2026-06-01", () => {
    const june1 = grid.find((c) => c.isCurrentMonth && c.day === 1)!;
    expect(grid.indexOf(june1)).toBe(1);
    expect(june1.month).toBe(5);
  });
  it("선행/후행 칸은 isCurrentMonth=false", () => {
    expect(grid[0].isCurrentMonth).toBe(false);
    expect(grid[41].isCurrentMonth).toBe(false);
  });
});

describe("ymd / monthPrefix", () => {
  it("month 0-indexed → YYYYMMDD/YYYYMM", () => {
    expect(ymd(2026, 5, 7)).toBe("20260607");
    expect(ymd(2026, 11, 25)).toBe("20261225");
    expect(monthPrefix(2026, 5)).toBe("202606");
  });
});

describe("한국어 명칭", () => {
  it("12개월·7요일", () => {
    expect(MONTH_NAMES_KO).toHaveLength(12);
    expect(MONTH_NAMES_KO[5]).toBe("6월");
    expect(WEEKDAYS_KO).toEqual(["일", "월", "화", "수", "목", "금", "토"]);
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `pnpm exec vitest run src/lib/calendar/month-grid.test.ts`
Expected: FAIL(import 실패).

- [ ] **Step 3: categories.ts**

```ts
// 일정 유형·색 단일정의. 색은 Tailwind 토큰 클래스(임의 hex 금지).
export type CalendarCategory = "미팅" | "임장" | "계약" | "기타";

export const CALENDAR_CATEGORIES: { value: CalendarCategory; dot: string; text: string }[] = [
  { value: "미팅", dot: "bg-blue-500", text: "text-blue-600" },
  { value: "임장", dot: "bg-emerald-500", text: "text-emerald-600" },
  { value: "계약", dot: "bg-amber-500", text: "text-amber-600" },
  { value: "기타", dot: "bg-slate-400", text: "text-slate-500" },
];

export const PROPERTY_EVENT_STYLE = { dot: "bg-red-500", text: "text-red-600" };

export function categoryStyle(value: string): { dot: string; text: string } {
  return CALENDAR_CATEGORIES.find((c) => c.value === value) ?? CALENDAR_CATEGORIES[3];
}
```

- [ ] **Step 4: month-grid.ts**

```ts
// 월 그리드·한국어 명칭·날짜키 (순수). month는 0-indexed(0=1월).
export const MONTH_NAMES_KO = ["1월", "2월", "3월", "4월", "5월", "6월", "7월", "8월", "9월", "10월", "11월", "12월"];
export const WEEKDAYS_KO = ["일", "월", "화", "수", "목", "금", "토"];

export type GridDay = { year: number; month: number; day: number; isCurrentMonth: boolean };

const daysInMonth = (year: number, month: number) => new Date(year, month + 1, 0).getDate();
const firstWeekday = (year: number, month: number) => new Date(year, month, 1).getDay();

export function buildMonthGrid(year: number, month: number): GridDay[] {
  const first = firstWeekday(year, month);
  const total = daysInMonth(year, month);
  const grid: GridDay[] = [];
  const prevM = month === 0 ? 11 : month - 1;
  const prevY = month === 0 ? year - 1 : year;
  const prevDays = daysInMonth(prevY, prevM);
  for (let i = first - 1; i >= 0; i--) grid.push({ year: prevY, month: prevM, day: prevDays - i, isCurrentMonth: false });
  for (let d = 1; d <= total; d++) grid.push({ year, month, day: d, isCurrentMonth: true });
  const nextM = month === 11 ? 0 : month + 1;
  const nextY = month === 11 ? year + 1 : year;
  let nd = 1;
  while (grid.length < 42) grid.push({ year: nextY, month: nextM, day: nd++, isCurrentMonth: false });
  return grid;
}

const pad = (n: number) => String(n).padStart(2, "0");
export const ymd = (year: number, month: number, day: number) => `${year}${pad(month + 1)}${pad(day)}`;
export const monthPrefix = (year: number, month: number) => `${year}${pad(month + 1)}`;
```

- [ ] **Step 5: 통과·커밋**

Run: `pnpm exec vitest run src/lib/calendar/month-grid.test.ts`
Expected: PASS.
```bash
git add src/lib/calendar/categories.ts src/lib/calendar/month-grid.ts src/lib/calendar/month-grid.test.ts
git commit -m "feat(calendar): 카테고리·월그리드(순수)+테스트"
```

---

## Task 3: 매물 파생 일정 (순수, TDD)

**Files:**
- Create: `src/lib/calendar/property-events.ts`, `src/lib/calendar/property-events.test.ts`

**Interfaces:**
- Produces:
  - `type PropertyDateEvent = { date: string; label: string; propertyId: string; field: string }`
  - `PROPERTY_DATE_FIELDS: { key: string; label: string }[]` (7개)
  - `propertyCalendarEvents(properties: PropForEvents[]): PropertyDateEvent[]` where `type PropForEvents = { id: string; name: string | null; complexName: string | null } & Record<string, unknown>`

- [ ] **Step 1: 실패 테스트**

`src/lib/calendar/property-events.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { propertyCalendarEvents, PROPERTY_DATE_FIELDS } from "./property-events";

describe("propertyCalendarEvents", () => {
  const props = [
    { id: "p1", name: "정자동 자이 102-1503", complexName: "정자동 자이", contractDate: "20260610", balanceDate: "20260701" },
    { id: "p2", name: null, complexName: "래미안", moveInDate: "20260615" },
    { id: "p3", name: "값없음", complexName: null }, // 날짜 없음 → 항목 없음
  ];
  const events = propertyCalendarEvents(props);
  it("날짜 있는 필드마다 1건·라벨=매물명+필드", () => {
    expect(events).toHaveLength(3);
    const e = events.find((x) => x.propertyId === "p1" && x.field === "balanceDate")!;
    expect(e.date).toBe("20260701");
    expect(e.label).toBe("정자동 자이 102-1503 잔금일");
  });
  it("매물명 없으면 단지명 사용", () => {
    const e = events.find((x) => x.propertyId === "p2")!;
    expect(e.label).toBe("래미안 입주일");
  });
  it("PROPERTY_DATE_FIELDS 7종(사용승인일 제외)", () => {
    expect(PROPERTY_DATE_FIELDS).toHaveLength(7);
    expect(PROPERTY_DATE_FIELDS.map((f) => f.key)).not.toContain("approvalDate");
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `pnpm exec vitest run src/lib/calendar/property-events.test.ts`
Expected: FAIL.

- [ ] **Step 3: property-events.ts**

```ts
// 매물 일정 날짜 → 캘린더 파생 항목(읽기전용). YYYYMMDD 값이 있는 필드만.
export type PropForEvents = { id: string; name: string | null; complexName: string | null } & Record<string, unknown>;
export type PropertyDateEvent = { date: string; label: string; propertyId: string; field: string };

export const PROPERTY_DATE_FIELDS: { key: string; label: string }[] = [
  { key: "contractHopeDate", label: "계약희망일" },
  { key: "contractDate", label: "계약일" },
  { key: "interim1Date", label: "중도금1" },
  { key: "interim2Date", label: "중도금2" },
  { key: "balanceDate", label: "잔금일" },
  { key: "moveInHopeDate", label: "입주희망일" },
  { key: "moveInDate", label: "입주일" },
];

export function propertyCalendarEvents(properties: PropForEvents[]): PropertyDateEvent[] {
  const out: PropertyDateEvent[] = [];
  for (const p of properties) {
    const name = (p.name || p.complexName || "매물") as string;
    for (const f of PROPERTY_DATE_FIELDS) {
      const v = p[f.key];
      if (typeof v === "string" && /^\d{8}$/.test(v)) {
        out.push({ date: v, label: `${name} ${f.label}`, propertyId: p.id, field: f.key });
      }
    }
  }
  return out;
}
```

- [ ] **Step 4: 통과·커밋**

Run: `pnpm exec vitest run src/lib/calendar/property-events.test.ts`
Expected: PASS.
```bash
git add src/lib/calendar/property-events.ts src/lib/calendar/property-events.test.ts
git commit -m "feat(calendar): 매물 날짜→파생 일정(순수)+테스트"
```

---

## Task 4: 서버 액션 (loadCalendar + CRUD)

**Files:**
- Create: `src/app/(dashboard)/dashboard/calendar/actions.ts`

**Interfaces:**
- Consumes: `propertyCalendarEvents`/`PropertyDateEvent` (T3), `monthPrefix` (T2), `db`, `getCurrentUser`.
- Produces:
  - `type EventInput = { title: string; date: string; startTime?: string | null; category: string; memo?: string | null; propertyId?: string | null; customerId?: string | null }`
  - `type EventRow = { id: string; title: string; date: string; startTime: string | null; category: string; memo: string | null; propertyId: string | null; propertyLabel: string | null; customerId: string | null; customerLabel: string | null }`
  - `type CalendarOption = { id: string; label: string }`
  - `loadCalendar(year: number, month: number): Promise<{ events: EventRow[]; propertyDates: PropertyDateEvent[]; properties: CalendarOption[]; customers: CalendarOption[] }>` (month 0-indexed)
  - `createEvent(input: EventInput): Promise<string>`
  - `updateEvent(id: string, input: EventInput): Promise<void>`
  - `deleteEvent(id: string): Promise<void>`

- [ ] **Step 1: actions.ts**

```ts
"use server";
import { revalidatePath } from "next/cache";

import { getCurrentUser } from "@/lib/auth/current-user";
import { db } from "@/lib/db";
import { monthPrefix } from "@/lib/calendar/month-grid";
import { propertyCalendarEvents, type PropertyDateEvent } from "@/lib/calendar/property-events";
import { CALENDAR_CATEGORIES } from "@/lib/calendar/categories";

export type EventInput = {
  title: string; date: string; startTime?: string | null; category: string;
  memo?: string | null; propertyId?: string | null; customerId?: string | null;
};
export type EventRow = {
  id: string; title: string; date: string; startTime: string | null; category: string;
  memo: string | null; propertyId: string | null; propertyLabel: string | null;
  customerId: string | null; customerLabel: string | null;
};
export type CalendarOption = { id: string; label: string };

async function requireUser() {
  const user = await getCurrentUser();
  if (!user) throw new Error("인증이 필요합니다");
  return user;
}

const CATS = new Set(CALENDAR_CATEGORIES.map((c) => c.value as string));

function toData(input: EventInput) {
  return {
    title: (input.title ?? "").trim(),
    date: String(input.date ?? "").replace(/[^0-9]/g, "").slice(0, 8),
    startTime: input.startTime?.trim() || null,
    category: CATS.has(input.category) ? input.category : "기타",
    memo: input.memo?.trim() || null,
  };
}

async function ownedId(model: "property" | "customer", userId: string, id?: string | null): Promise<string | null> {
  if (!id) return null;
  const row = model === "property"
    ? await db.property.findFirst({ where: { id, userId }, select: { id: true } })
    : await db.customer.findFirst({ where: { id, userId }, select: { id: true } });
  return row ? row.id : null;
}

export async function loadCalendar(year: number, month: number): Promise<{
  events: EventRow[]; propertyDates: PropertyDateEvent[]; properties: CalendarOption[]; customers: CalendarOption[];
}> {
  const user = await requireUser();
  const prefix = monthPrefix(year, month);
  const rows = await db.event.findMany({
    where: { userId: user.id, date: { startsWith: prefix } },
    orderBy: [{ date: "asc" }, { startTime: "asc" }],
    include: { property: { select: { name: true, complexName: true } }, customer: { select: { name: true } } },
  });
  const events: EventRow[] = rows.map((r) => ({
    id: r.id, title: r.title, date: r.date, startTime: r.startTime, category: r.category, memo: r.memo,
    propertyId: r.propertyId, propertyLabel: r.property?.name ?? r.property?.complexName ?? null,
    customerId: r.customerId, customerLabel: r.customer?.name ?? null,
  }));

  // 매물 파생 일정(그 달) + 연결용 옵션
  const props = await db.property.findMany({
    where: { userId: user.id },
    select: {
      id: true, name: true, complexName: true,
      contractHopeDate: true, contractDate: true, interim1Date: true, interim2Date: true,
      balanceDate: true, moveInHopeDate: true, moveInDate: true,
    },
  });
  const propertyDates = propertyCalendarEvents(props).filter((e) => e.date.startsWith(prefix));
  const properties: CalendarOption[] = props.map((p) => ({ id: p.id, label: p.name ?? p.complexName ?? "매물" }));
  const customerRows = await db.customer.findMany({ where: { userId: user.id }, select: { id: true, name: true, phone: true } });
  const customers: CalendarOption[] = customerRows.map((c) => ({ id: c.id, label: c.phone ? `${c.name} (${c.phone})` : c.name }));

  return { events, propertyDates, properties, customers };
}

export async function createEvent(input: EventInput): Promise<string> {
  const user = await requireUser();
  const data = toData(input);
  if (!data.title || data.date.length !== 8) throw new Error("제목과 날짜는 필수입니다");
  const propertyId = await ownedId("property", user.id, input.propertyId);
  const customerId = await ownedId("customer", user.id, input.customerId);
  const e = await db.event.create({ data: { ...data, userId: user.id, propertyId, customerId } });
  revalidatePath("/dashboard/calendar");
  return e.id;
}

export async function updateEvent(id: string, input: EventInput): Promise<void> {
  const user = await requireUser();
  const data = toData(input);
  if (!data.title || data.date.length !== 8) throw new Error("제목과 날짜는 필수입니다");
  const propertyId = await ownedId("property", user.id, input.propertyId);
  const customerId = await ownedId("customer", user.id, input.customerId);
  await db.event.updateMany({ where: { id, userId: user.id }, data: { ...data, propertyId, customerId } });
  revalidatePath("/dashboard/calendar");
}

export async function deleteEvent(id: string): Promise<void> {
  const user = await requireUser();
  await db.event.deleteMany({ where: { id, userId: user.id } });
  revalidatePath("/dashboard/calendar");
}
```

- [ ] **Step 2: 정적검증·커밋**

Run: `pnpm exec tsc --noEmit && pnpm exec eslint "src/app/(dashboard)/dashboard/calendar/actions.ts"`
Expected: 0/0. (`db.event`·복합 select가 generate된 클라이언트와 일치 — 불일치 시 tsc가 잡음.)
```bash
git add "src/app/(dashboard)/dashboard/calendar/actions.ts"
git commit -m "feat(calendar): loadCalendar·이벤트 CRUD 서버액션"
```

---

## Task 5: 페이지·월그리드·사이드바

**Files:**
- Create: `src/app/(dashboard)/dashboard/calendar/page.tsx`, `src/app/(dashboard)/dashboard/calendar/calendar-view.tsx`

**Interfaces:**
- Consumes: `loadCalendar`/`EventRow`/`CalendarOption`/`PropertyDateEvent` (T4), `buildMonthGrid`/`ymd`/`monthPrefix`/`MONTH_NAMES_KO`/`WEEKDAYS_KO` (T2), `CALENDAR_CATEGORIES`/`PROPERTY_EVENT_STYLE`/`categoryStyle` (T2), `getCurrentUser`.
- Produces: 라우트 `/dashboard/calendar`. (이벤트 추가/수정은 Task 6에서 다이얼로그로.)

- [ ] **Step 1: 템플릿 패턴 확인**

Read: `src/components/apps/calendar/calendar-app.tsx`(월 그리드·사이드바·네비 구조 — 참고만, 데이터는 DB), `src/components/ui/`의 `button`·`card`·`checkbox`·`separator` 사용법.

- [ ] **Step 2: page.tsx (서버, 보호)**

```tsx
import { redirect } from "next/navigation";
import { getCurrentUser } from "@/lib/auth/current-user";
import { CalendarView } from "./calendar-view";

export default async function CalendarPage() {
  if (!(await getCurrentUser())) redirect("/login");
  return <CalendarView />;
}
```

- [ ] **Step 3: calendar-view.tsx (클라)**

`calendar-app.tsx`의 월 그리드 구조를 미러링하되 **DB 연동·한국어·실제 오늘**. 요구사항:
- `"use client"`. 상태: `view`({year, month}; 초기값은 `new Date()`로 실제 이번 달, month 0-indexed), `data`(loadCalendar 결과 또는 null), `loading`, `enabled`(표시할 유형 Set: CALENDAR_CATEGORIES value들 + "매물"), `selectedDate`(클릭한 YYYYMMDD 또는 null).
- 마운트·`view` 변경 시 `loadCalendar(view.year, view.month)` 호출해 `data` 갱신.
- 헤더: `{MONTH_NAMES_KO[month]} {year}` + 이전/다음/오늘 버튼(오늘=`new Date()`의 연·월). 우측 "일정 추가" 버튼(Task 6에서 다이얼로그 연결 — 이번엔 `onClick`만, 동작은 Task 6).
- 그리드: `buildMonthGrid(year, month)` 42칸, 요일 헤더 `WEEKDAYS_KO`. 각 칸의 `cellYmd = ymd(c.year, c.month, c.day)`:
  - 수기 일정: `data.events.filter(e => e.date === cellYmd && enabled.has(e.category))` → 점(categoryStyle.dot)+제목.
  - 매물 파생: `data.propertyDates.filter(e => e.date === cellYmd)` (enabled.has("매물")일 때) → 점(PROPERTY_EVENT_STYLE.dot)+라벨.
  - 넘치면 "+N". 오늘 칸 강조(실제 오늘과 같으면).
  - 칸 클릭 → `selectedDate = cellYmd`.
- 좌측 사이드바: 유형별 체크박스(미팅·임장·계약·기타 + 매물 일정) — `enabled` 토글. 색 점 표시.
- 우측/하단 패널: `selectedDate`가 있으면 그날 일정 목록(수기+파생). 수기 일정 클릭 → (Task 6) 수정 다이얼로그. 매물 파생 클릭 → `router.push(\`/dashboard/properties/${propertyId}/edit\`)`.
- Base UI primitive만, 임의 px/hex 금지(색은 categories의 토큰 클래스). UI 카피 한국어.

(전체 JSX·레이아웃은 calendar-app 구조를 따른다. 미니월·정확한 픽셀은 템플릿 참고.)

- [ ] **Step 4: 정적검증·빌드·커밋**

Run: `pnpm exec tsc --noEmit && pnpm exec eslint "src/app/(dashboard)/dashboard/calendar" && pnpm build`
Expected: 0/0, 빌드 성공.
```bash
git add "src/app/(dashboard)/dashboard/calendar/page.tsx" "src/app/(dashboard)/dashboard/calendar/calendar-view.tsx"
git commit -m "feat(calendar): 월 캘린더 뷰·매물 일정 표시·유형 필터"
```

---

## Task 6: 일정 추가/수정/삭제 다이얼로그

**Files:**
- Create: `src/app/(dashboard)/dashboard/calendar/event-dialog.tsx`
- Modify: `src/app/(dashboard)/dashboard/calendar/calendar-view.tsx` (다이얼로그 연결)

**Interfaces:**
- Consumes: `createEvent`/`updateEvent`/`deleteEvent`/`EventRow`/`EventInput`/`CalendarOption` (T4), `CALENDAR_CATEGORIES` (T2).

- [ ] **Step 1: 다이얼로그 프리미티브 확인**

Read: `src/components/ui/dialog.tsx`(Dialog 구성), `src/components/ui/native-select.tsx`, 기존 폼(`customers/customer-form.tsx`)의 입력·저장 패턴.

- [ ] **Step 2: event-dialog.tsx (클라)**

요구사항:
- props `{ open: boolean; onOpenChange: (o: boolean) => void; event?: EventRow; defaultDate?: string; properties: CalendarOption[]; customers: CalendarOption[]; onSaved: () => void }`.
- 필드: 제목(Input)·날짜(date input, YYYY-MM-DD↔YYYYMMDD 변환)·종일 토글+시간(Input "HH:MM", 종일이면 비활성/null)·유형(NativeSelect: CALENDAR_CATEGORIES)·매물 연결(NativeSelect: properties, 빈 선택 허용)·고객 연결(NativeSelect: customers, 빈 선택)·메모(Textarea).
- 신규(`event` 없음): 초기 날짜=`defaultDate`, 유형=기타. 수정(`event` 있음): 그 값으로 채움.
- 저장: 신규 `createEvent(input)`, 수정 `updateEvent(event.id, input)` → `onSaved()`(상위에서 loadCalendar 재호출) + 닫기. 삭제(수정일 때만): `deleteEvent(event.id)` → `onSaved()` + 닫기. 제목·날짜 비면 막기, 에러 toast.
- `Dialog`/`DialogContent`/`DialogHeader`/`DialogFooter` 사용, 한국어, 토큰만.

- [ ] **Step 3: calendar-view.tsx 연결**

- 상태 추가: `dialogOpen`, `editing`(EventRow|null), `dialogDate`(string|null).
- "일정 추가" 버튼 → `setEditing(null); setDialogDate(selectedDate ?? ymd(오늘)); setDialogOpen(true)`.
- 수기 일정 클릭 → `setEditing(event); setDialogOpen(true)`.
- `<EventDialog open={dialogOpen} onOpenChange={setDialogOpen} event={editing ?? undefined} defaultDate={dialogDate ?? undefined} properties={data.properties} customers={data.customers} onSaved={() => loadCalendar 재호출} />`.

- [ ] **Step 4: 정적검증·빌드·커밋**

Run: `pnpm exec tsc --noEmit && pnpm exec eslint "src/app/(dashboard)/dashboard/calendar" && pnpm build`
Expected: 0/0, 빌드 성공.
```bash
git add "src/app/(dashboard)/dashboard/calendar/event-dialog.tsx" "src/app/(dashboard)/dashboard/calendar/calendar-view.tsx"
git commit -m "feat(calendar): 일정 추가·수정·삭제 다이얼로그"
```

---

## 마감 (전체 통합 후)

- [ ] `pnpm exec vitest run`(전체 그린)·`pnpm build` 확인.
- [ ] README §현재 반영 상태 + PROJECT_GUIDE §현재 참고 상태에 기능 요약 1줄(CLAUDE.md §5).
- [ ] ⚠️ 머지 전 라이브 1회: 월 이동·매물 일정 표시·일정 추가/수정/삭제·유형 필터·매물 일정 클릭→매물 이동 렌더 확인.
- [ ] spec·plan 포함 commit, push는 사용자 요청 시.
